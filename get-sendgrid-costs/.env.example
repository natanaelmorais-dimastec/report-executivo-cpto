# Twilio account credentials (SendGrid via Twilio billing).
# Find at: twilio.com -> Console -> Account Info (top-right of dashboard).
# Use the LIVE Account SID + Auth Token (not test creds).
#
# Copy to .env (already gitignored).

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
